ConvertFrom-StringData @'
    VMRunningDesired = Desired state for the VM is running state.
    ResumeVM = Resuming VM from paused state.
    StartVM = Starting VM from off state.
    VMPausedDesired = Desired state for the VM is paused state.
    SuspendVM = Pausing VM.
    StopVM = Stopping VM.
    EnableService = Enabling {0}.
    DisableService = Disabling {0}.
    MappingExists = VM Network adapter mapping exists.
    MappingDoesNotExist = VM Network adapter mapping does not exist.
    MappingNeedsAnUpdate = VM Network adapter mapping needs an update.
'@
